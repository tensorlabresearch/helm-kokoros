apiVersion: v1
kind: Service
metadata:
  name: {{ include "kokoros.fullname" . }}
  labels:
    {{- include "kokoros.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kokoros.selectorLabels" . | nindent 4 }}
