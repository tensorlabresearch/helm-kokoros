{{- $bundle := dict -}}
{{- $bundleFound := false -}}
{{- range $candidate := .Values.kokoros.bundles -}}
  {{- if eq $candidate.name $.Values.kokoros.activeBundle -}}
    {{- $bundle = $candidate -}}
    {{- $bundleFound = true -}}
  {{- end -}}
{{- end -}}
{{- if not $bundleFound -}}
{{- fail (printf "kokoros.activeBundle %q does not exist in kokoros.bundles" .Values.kokoros.activeBundle) -}}
{{- end -}}
{{- $resources := deepCopy (default (dict) .Values.resources) -}}
{{- if not (hasKey $resources "requests") -}}{{- $_ := set $resources "requests" (dict) -}}{{- end -}}
{{- if not (hasKey $resources "limits") -}}{{- $_ := set $resources "limits" (dict) -}}{{- end -}}
{{- if .Values.gpu.enabled -}}
{{- $_ := set $resources.requests .Values.gpu.resourceKey .Values.gpu.count -}}
{{- $_ := set $resources.limits .Values.gpu.resourceKey .Values.gpu.count -}}
{{- end -}}
{{- $bundlePreload := default (list) $bundle.preload -}}
{{- $hasPreload := or (gt (len .Values.modelPreload.items) 0) (gt (len $bundlePreload) 0) -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kokoros.fullname" . }}
  labels:
    {{- include "kokoros.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "kokoros.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "kokoros.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "kokoros.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- if and .Values.modelPreload.enabled $hasPreload }}
      initContainers:
        - name: preload-models
          image: "{{ .Values.modelPreload.image.repository }}:{{ .Values.modelPreload.image.tag }}"
          imagePullPolicy: {{ .Values.modelPreload.image.pullPolicy }}
          command: ["/bin/sh", "-ec"]
          args:
            - |
              set -eu

              CONTINUE_ON_ERROR="{{ ternary "true" "false" .Values.modelPreload.continueOnError }}"

              ensure_deps() {
                if ! command -v curl >/dev/null 2>&1; then
                  echo "curl not present; installing runtime deps"
                  apk add --no-cache curl coreutils >/dev/null
                fi
              }

              download_model() {
                item_id="$1"
                item_url="$2"
                item_subdir="$3"
                item_filename="$4"
                item_sha="$5"

                target_dir="{{ .Values.persistence.models.mountPath }}/$item_subdir"
                target_file="$target_dir/$item_filename"
                tmp_file="$target_file.part"

                mkdir -p "$target_dir"

                if [ -f "$target_file" ]; then
                  echo "[$item_id] exists: $target_file"
                  return 0
                fi

                echo "[$item_id] downloading $item_url"
                curl -fsSL --retry 5 --retry-all-errors "$item_url" -o "$tmp_file"

                if [ -n "$item_sha" ]; then
                  echo "[$item_id] verifying sha256"
                  echo "$item_sha  $tmp_file" | sha256sum -c -
                fi

                mv "$tmp_file" "$target_file"
                echo "[$item_id] ready: $target_file"
              }

              run_download() {
                if download_model "$1" "$2" "$3" "$4" "$5"; then
                  return 0
                fi

                if [ "$CONTINUE_ON_ERROR" = "true" ]; then
                  echo "[warn] preload failed for $1; continuing"
                  return 0
                fi

                echo "[error] preload failed for $1"
                exit 1
              }

              ensure_deps

              {{- range .Values.modelPreload.items }}
              run_download {{ .id | quote }} {{ .url | quote }} {{ .targetSubdir | quote }} {{ .filename | quote }} {{ .sha256 | quote }}
              {{- end }}

              {{- range $bundlePreload }}
              run_download {{ .id | quote }} {{ .url | quote }} {{ .targetSubdir | quote }} {{ .filename | quote }} {{ .sha256 | quote }}
              {{- end }}
          volumeMounts:
            - name: models
              mountPath: {{ .Values.persistence.models.mountPath }}
      {{- end }}
      containers:
        - name: kokoros
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          command: ["/usr/local/bin/koko"]
          args:
            - "--instances"
            - {{ .Values.kokoros.instances | quote }}
            - "--model"
            - {{ printf "%s/%s" .Values.persistence.models.mountPath $bundle.modelPath | quote }}
            - "--data"
            - {{ printf "%s/%s" .Values.persistence.models.mountPath $bundle.voicesPath | quote }}
            - "openai"
            - "--ip"
            - "0.0.0.0"
            - "--port"
            - {{ .Values.service.port | quote }}
            {{- range .Values.kokoros.extraArgs }}
            - {{ . | quote }}
            {{- end }}
          env:
            - name: RUST_LOG
              value: {{ .Values.kokoros.logLevel | quote }}
            {{- range .Values.kokoros.env }}
            - name: {{ .name }}
              value: {{ .value | quote }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          startupProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: {{ .Values.probes.startup.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.startup.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.startup.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
          resources:
            {{- toYaml $resources | nindent 12 }}
          volumeMounts:
            - name: models
              mountPath: {{ .Values.persistence.models.mountPath }}
              readOnly: {{ .Values.persistence.models.readOnly }}
      volumes:
        - name: models
          {{- if and .Values.persistence.models.enabled (not .Values.persistence.models.existingClaim) }}
          persistentVolumeClaim:
            claimName: {{ include "kokoros.fullname" . }}-models
          {{- else if .Values.persistence.models.existingClaim }}
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.models.existingClaim }}
          {{- else }}
          emptyDir: {}
          {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
